@using MudBlazor
@using Model


<MudTimeline>
    @foreach (var workExp in workExperience)
    {
        @if (workExp != null)
        {
            <MudTimelineItem Color="Color.Warning">
                <ItemOpposite>
                    <MudText Typo="Typo.h5" style="color:lemonchiffon">@workExp.start_time - @workExp.end_time</MudText>
                </ItemOpposite>
                <ItemContent>
                    <MudText Typo="Typo.h5" GutterBottom="true">@workExp.company_name</MudText>
                    <MudText Typo="Typo.h6" style="color:lemonchiffon" GutterBottom="true">@workExp.title</MudText>
                    <MudText>@workExp.work_description</MudText>
                </ItemContent>
            </MudTimelineItem>
        
        }
        
    }
    
</MudTimeline>

@code {
    [Parameter]
    public List<WorkExperienceModel> workExperience { get; set; } = new List<WorkExperienceModel>();

}
