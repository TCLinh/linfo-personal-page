@using Model
@using Helper
@using System.Text;
@inject HttpClient Http

@page "/"

<PageTitle>Home</PageTitle>


<div class="personal-info">
    <div class="avatar-container">
        <img src="/img/avatar.jpg" alt="avatar"/>
    </div>
   
    <div class="personal-content">
        <MudText Typo="Typo.h3" Align="Align.End">
            Hello, I am <span style="color:lemonchiffon">@displayedTitle</span>
            <span style="visibility:@(isCursorVisible ? "visible" : "hidden"); color:lemonchiffon">_</span>
        </MudText>
        <div class="about-me-description">
            @personalDetails?.about_me
        </div>
        
    </div>

    <div class="info">
        <div>
            <MudIcon Icon="@Icons.Material.Filled.Person"/>
            @personalDetails?.full_name
        </div>
        <div>
            <MudIcon Icon="@Icons.Material.Filled.CalendarToday"/>
            @personalDetails?.dob
        </div>
        <div>
            <MudSnackbarProvider />
            <MudIcon Icon="@Icons.Material.Filled.Email" />
            @personalDetails?.email
        </div>
    </div>
</div>



<div class="mt-5">
    <div class="title">
        <img src="img/sparkle.png" />
        <div class="title-container">
            <MudText Typo="Typo.h4" Align="Align.Center" class="p-2">Skills</MudText>
        </div>
    </div>
    <div class="d-flex justify-content-center w-100">
        <div class="d-flex flex-column w-75 align-items-center">
            @foreach (var skillType in Constant.SKILL_TYPES)
            {
                <div class="d-flex flex-row mt-5 w-100">
                    <div class="skill-type">
                        <MudText Typo="Typo.h5" Align="Align.Center" class="p-2">@skillType</MudText>
                    </div>
                    <div class="skill-list d-flex flex-column align-content-center pl-10">
                        @if (personalDetails?.skills != null && personalDetails.skills.Any())
                        {
                            var skillFiltered = personalDetails.skills.Where(x => x.skill_type == skillType);
                            @foreach (var skill in skillFiltered)
                            {
                                <div class="row">
                                    <div class="col-1 d-flex align-items-center justify-content-end">
                                        <MudIcon Icon="@Icons.Material.Filled.Star" Size="Size.Small" />
                                    </div>
                                    <div class="skill col-3">
                                        @skill.skill_name
                                    </div>
                                    <div class="skill col-6">
                                        @skill.skill_level YOE
                                    </div>
                                </div>
                            }
                        }
                    </div>
                </div>
            }
        </div>
    </div>
</div>

<div class="mt-5">
    <div class="title">
        <img src="img/sparkle.png" />
        <div class="title-container">
            <MudText Typo="Typo.h4" class="p-2" Align="Align.Center">Work Experience</MudText>
        </div>
    </div>
    <div class="d-flex justify-content-center mt-5">
        <Timeline workExperience=@personalDetails?.work_experience />
    </div>
</div>

@*<div class="mt-5">
    <div class="title">
        <img src="img/sparkle.png" />
        <div class="title-container">
            <MudText Typo="Typo.h4" class="p-2" Align="Align.Center">Degrees &Aacute; Certifications</MudText>
        </div>
    </div>
    <div class="d-flex justify-content-center mt-5">
        TBU when I actually have certs that arent just uni diploma
    </div>
</div>*@

@code {
    [Inject] IJSRuntime JS { get; set; }

    private PersonalDetailsModel? personalDetails = new PersonalDetailsModel();
    protected override async Task OnInitializedAsync()
    {
        personalDetails = await Http.GetFromJsonAsync<PersonalDetailsModel>("sample-data/personal_info_en.json");

        var typingTask = StartTypingEffect();
        var cursorTask = BlinkCursor();

        await Task.WhenAll(typingTask, cursorTask);
    }
    
    private string[] titles = { "a Programmer", "a Developer", "a Thinker", "an Art Hobbyist", "an Animal Enthusiast" };
    private string displayedTitle = "";
    private int currentWordIndex = 0;
    private bool isCursorVisible = true;

    private async Task StartTypingEffect()
    {
        while (true)
        {
            string currentWord = titles[currentWordIndex];
            var displayedTitleBuilder = new StringBuilder();

            // Typing each character
            foreach (char letter in currentWord)
            {
                displayedTitleBuilder.Append(letter);
                displayedTitle = displayedTitleBuilder.ToString();

                StateHasChanged(); // Update UI after adding each letter
                await Task.Delay(100);
            }

            // Pause after typing the word
            await Task.Delay(2000);

            // Deleting each character
            for (int i = currentWord.Length - 1; i >= 0; i--)
            {
                displayedTitleBuilder.Remove(i, 1);
                displayedTitle = displayedTitleBuilder.ToString();

                StateHasChanged();
                await Task.Delay(50);
            }

            await Task.Delay(500);

            // Move to the next word
            currentWordIndex = (currentWordIndex + 1) % titles.Length;
        }
    }

    private async Task BlinkCursor()
    {
        while (true)
        {
            isCursorVisible = !isCursorVisible;
            StateHasChanged();
            await Task.Delay(500);
        }
    }

    private async Task CopyToClipboard()
    {
        if (personalDetails?.email != null)
        {
            await JS.InvokeVoidAsync("navigator.clipboard.writeText", personalDetails.email);
        }
    }
}